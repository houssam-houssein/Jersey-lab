# Copy to .env and fill in real values. Do not commit .env.

# --- Database (MongoDB Atlas) ---
# Get from: Atlas → Cluster → Connect → Connect your application → copy connection string
# Replace <password> with your database user password. Database name can stay or use Jerseylab
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/Jerseylab?retryWrites=true&w=majority

# --- Email (GoDaddy domain email) ---
EMAIL_SERVICE=godaddy
EMAIL_USER=info@yourdomain.com
EMAIL_PASSWORD=your-email-password
EMAIL_FROM="JerseyLab <info@yourdomain.com>"

# Optional if using generic SMTP instead of godaddy:
# SMTP_HOST=smtpout.secureserver.net
# SMTP_PORT=587
# SMTP_SECURE=false
